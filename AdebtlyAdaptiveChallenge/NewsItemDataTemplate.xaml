<UserControl
    x:Class="AdebtlyAdaptiveChallenge.NewsItemDataTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdebtlyAdaptiveChallenge"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">


        <StackPanel Margin="20" HorizontalAlignment="Stretch">
            <Image Source="{x:Bind NewsItem.Image}" HorizontalAlignment="Center" Width="300"/>
            <TextBlock Text="{x:Bind NewsItem.Headline}" HorizontalAlignment="Center" FontSize="16" />
            <TextBlock Text="{x:Bind NewsItem.Subhead}" HorizontalAlignment="Center" FontSize="11"/>
        </StackPanel>

</UserControl>
